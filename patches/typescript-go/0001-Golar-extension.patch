From 7e38037691314ffd8a5c2c51fddfb5c711909218 Mon Sep 17 00:00:00 2001
From: auvred <aauvred@gmail.com>
Date: Wed, 25 Mar 2026 08:55:38 +0300
Subject: [PATCH 1/3] Golar extension

---
 pkg/ast/ast.go                      | 15 +++++++++++++++
 pkg/compiler/program.go             | 30 ++++++++++++++++++++++++++++-
 pkg/diagnostics/diagnostics.go      |  9 +++++++++
 pkg/execute/incremental/snapshot.go | 11 ++++++-----
 pkg/module/resolver.go              | 21 ++++++++++++++++++++
 pkg/module/util.go                  | 10 ++++++++++
 pkg/tspath/extension.go             | 26 +++++++++++++++++++++++++
 pkg/vfs/osvfs/os.go                 |  9 +++++++--
 8 files changed, 123 insertions(+), 8 deletions(-)

diff --git a/pkg/ast/ast.go b/pkg/ast/ast.go
index e1530e18d..33fd8f2c5 100644
--- a/pkg/ast/ast.go
+++ b/pkg/ast/ast.go
@@ -71,6 +71,9 @@ func NewNodeFactory(hooks NodeFactoryHooks) *NodeFactory {
 	return &NodeFactory{hooks: hooks}
 }
 
+func NewNode(kind Kind, data nodeData) *Node {
+	return newNode(kind, data, NodeFactoryHooks{})
+}
 func newNode(kind Kind, data nodeData, hooks NodeFactoryHooks) *Node {
 	n := data.AsNode()
 	n.Loc = core.UndefinedTextRange()
@@ -2471,6 +2474,10 @@ type SourceFile struct {
 	LocalsContainerBase
 	CompositeBase
 
+	GolarLanguageData       any
+	WrapSemanticDiagnostics func([]*Diagnostic) []*Diagnostic
+	WrapDiagnostics         func([]*Diagnostic) []*Diagnostic
+
 	// Fields set by NewSourceFile
 	fileName       string // For debugging convenience
 	parseOptions   SourceFileParseOptions
@@ -2569,10 +2576,18 @@ func (node *SourceFile) Text() string {
 	return node.text
 }
 
+func (node *SourceFile) SetText(text string) {
+	node.text = text
+}
+
 func (node *SourceFile) FileName() string {
 	return node.parseOptions.FileName
 }
 
+func (node *SourceFile) SetParseOptions(opts SourceFileParseOptions) {
+	node.parseOptions = opts
+}
+
 func (node *SourceFile) Path() tspath.Path {
 	return node.parseOptions.Path
 }
diff --git a/pkg/compiler/program.go b/pkg/compiler/program.go
index 1436c8d0a..66f4d1249 100644
--- a/pkg/compiler/program.go
+++ b/pkg/compiler/program.go
@@ -266,7 +266,19 @@ func (p *Program) GetSourceFileFromReference(origin *ast.SourceFile, ref *ast.Fi
 	return nil
 }
 
+type golarExt struct {
+	WrapCompilerHost    func(base CompilerHost, configName string) CompilerHost
+	ReportingSourceFile func(file *ast.SourceFile) *ast.SourceFile
+}
+
+var GolarExt = &golarExt{}
+
 func NewProgram(opts ProgramOptions) *Program {
+	configName := ""
+	if opts.Config != nil && opts.Config.ConfigFile != nil && opts.Config.ConfigFile.SourceFile != nil {
+		configName = opts.Config.ConfigFile.SourceFile.FileName()
+	}
+	opts.Host = GolarExt.WrapCompilerHost(opts.Host, configName)
 	p := &Program{opts: opts}
 	if p.opts.Tracing != nil {
 		defer p.opts.Tracing.Push(tracing.PhaseProgram, "createProgram", map[string]any{"configFilePath": opts.Config.CompilerOptions().ConfigFilePath}, true)()
@@ -605,6 +617,9 @@ func (p *Program) GetSyntacticDiagnostics(ctx context.Context, sourceFile *ast.S
 		if ast.IsSourceFileJS(file) && !ast.IsCheckJSEnabledForFile(file, p.Options()) {
 			diags = append(diags, getAdditionalJSSyntacticDiagnostics(file, p.Options())...)
 		}
+		if file.WrapDiagnostics != nil {
+			diags = file.WrapDiagnostics(diags)
+		}
 		return diags
 	})
 }
@@ -647,7 +662,11 @@ func (p *Program) GetBindDiagnostics(ctx context.Context, sourceFile *ast.Source
 		p.BindSourceFiles()
 	}
 	return p.collectDiagnostics(ctx, sourceFile, false /*concurrent*/, func(_ context.Context, file *ast.SourceFile) []*ast.Diagnostic {
-		return file.BindDiagnostics()
+		diags := file.BindDiagnostics()
+		if file.WrapDiagnostics != nil {
+			diags = file.WrapDiagnostics(diags)
+		}
+		return diags
 	})
 }
 
@@ -680,6 +699,9 @@ func (p *Program) GetIncludeProcessorDiagnostics(sourceFile *ast.SourceFile) []*
 		return nil
 	}
 	filtered, _ := p.getDiagnosticsWithPrecedingDirectives(sourceFile, p.includeProcessor.getDiagnostics(p).GetDiagnosticsForFile(sourceFile.FileName()))
+	if sourceFile.WrapDiagnostics != nil {
+		filtered = sourceFile.WrapDiagnostics(filtered)
+	}
 	return filtered
 }
 
@@ -1334,6 +1356,9 @@ func (p *Program) getBindAndCheckDiagnosticsWithChecker(ctx context.Context, fil
 
 	isPlainJS := ast.IsPlainJSFile(sourceFile, compilerOptions.CheckJs)
 	if isPlainJS {
+		if sourceFile.WrapSemanticDiagnostics != nil {
+			diags = sourceFile.WrapSemanticDiagnostics(diags)
+		}
 		return core.Filter(diags, func(d *ast.Diagnostic) bool {
 			return plainJSErrors.Has(d.Code())
 		})
@@ -1353,6 +1378,9 @@ func (p *Program) getBindAndCheckDiagnosticsWithChecker(ctx context.Context, fil
 			filtered = append(filtered, ast.NewDiagnostic(sourceFile, directive.Loc, diagnostics.Unused_ts_expect_error_directive))
 		}
 	}
+	if sourceFile.WrapSemanticDiagnostics != nil {
+		filtered = sourceFile.WrapSemanticDiagnostics(filtered)
+	}
 	return filtered
 }
 
diff --git a/pkg/diagnostics/diagnostics.go b/pkg/diagnostics/diagnostics.go
index ac7809a1e..b5385bf58 100644
--- a/pkg/diagnostics/diagnostics.go
+++ b/pkg/diagnostics/diagnostics.go
@@ -59,6 +59,15 @@ func (m *Message) ReportsUnnecessary() bool           { return m.reportsUnnecess
 func (m *Message) ElidedInCompatibilityPyramid() bool { return m.elidedInCompatibilityPyramid }
 func (m *Message) ReportsDeprecated() bool            { return m.reportsDeprecated }
 
+func NewMessage(code int32, category Category, key Key, text string) *Message {
+	return &Message{
+		code:     code,
+		category: category,
+		key:      key,
+		text:     text,
+	}
+}
+
 // For debugging only.
 func (m *Message) String() string {
 	return m.text
diff --git a/pkg/execute/incremental/snapshot.go b/pkg/execute/incremental/snapshot.go
index cf7b73d63..378dad6b1 100644
--- a/pkg/execute/incremental/snapshot.go
+++ b/pkg/execute/incremental/snapshot.go
@@ -154,12 +154,13 @@ type DiagnosticsOrBuildInfoDiagnosticsWithFileName struct {
 }
 
 func (b *buildInfoDiagnosticWithFileName) toDiagnostic(p *compiler.Program, file *ast.SourceFile) *ast.Diagnostic {
-	var fileForDiagnostic *ast.SourceFile
+	var rawFileForDiagnostic *ast.SourceFile
 	if b.file != "" {
-		fileForDiagnostic = p.GetSourceFileByPath(b.file)
+		rawFileForDiagnostic = p.GetSourceFileByPath(b.file)
 	} else if !b.noFile {
-		fileForDiagnostic = file
+		rawFileForDiagnostic = file
 	}
+	fileForDiagnostic := compiler.GolarExt.ReportingSourceFile(rawFileForDiagnostic)
 
 	if b.repopulateInfo != nil {
 		return repopulateDiagnosticChain(b, p, fileForDiagnostic)
@@ -167,11 +168,11 @@ func (b *buildInfoDiagnosticWithFileName) toDiagnostic(p *compiler.Program, file
 
 	var messageChain []*ast.Diagnostic
 	for _, msg := range b.messageChain {
-		messageChain = append(messageChain, msg.toDiagnostic(p, fileForDiagnostic))
+		messageChain = append(messageChain, msg.toDiagnostic(p, rawFileForDiagnostic))
 	}
 	var relatedInformation []*ast.Diagnostic
 	for _, info := range b.relatedInformation {
-		relatedInformation = append(relatedInformation, info.toDiagnostic(p, fileForDiagnostic))
+		relatedInformation = append(relatedInformation, info.toDiagnostic(p, rawFileForDiagnostic))
 	}
 	return ast.NewDiagnosticFromSerialized(
 		fileForDiagnostic,
diff --git a/pkg/module/resolver.go b/pkg/module/resolver.go
index 1fa1a51e3..aecf775db 100644
--- a/pkg/module/resolver.go
+++ b/pkg/module/resolver.go
@@ -1552,8 +1552,29 @@ func (r *resolutionState) tryAddingExtensions(extensionless string, extensions e
 				return resolved
 			}
 		}
+		if originalExtension == "" {
+			for _, e := range tspath.ExtraExtensionless {
+				if resolved := r.tryExtension(e, extensionless, false); !resolved.shouldContinueSearching() {
+					return resolved
+				}
+			}
+		}
 		return continueSearching()
 	default:
+		for _, e := range tspath.ExtraExtensions {
+			if e == originalExtension {
+				if extensions&extensionsDeclaration != 0 && !tspath.IsDeclarationFileName(extensionless+originalExtension) {
+					if resolved := r.tryExtension(".d"+originalExtension+".ts", extensionless, false); !resolved.shouldContinueSearching() {
+						return resolved
+					}
+				}
+				if resolved := r.tryExtension(originalExtension, extensionless, false); !resolved.shouldContinueSearching() {
+					return resolved
+				}
+				return continueSearching()
+			}
+		}
+
 		if extensions&extensionsDeclaration != 0 && !tspath.IsDeclarationFileName(extensionless+originalExtension) {
 			if resolved := r.tryExtension(".d"+originalExtension+".ts", extensionless, false); !resolved.shouldContinueSearching() {
 				return resolved
diff --git a/pkg/module/util.go b/pkg/module/util.go
index fe0e514e2..f88e5ba91 100644
--- a/pkg/module/util.go
+++ b/pkg/module/util.go
@@ -169,6 +169,11 @@ func GetResolutionDiagnostic(options *core.CompilerOptions, resolvedModule *Reso
 	case tspath.ExtensionJson:
 		return needResolveJsonModule()
 	default:
+		for _, e := range tspath.ExtraExtensions {
+			if e == resolvedModule.Extension {
+				return nil
+			}
+		}
 		return needAllowArbitraryExtensions()
 	}
 }
@@ -192,6 +197,11 @@ func TryGetJSExtensionForFile(fileName string, options *core.CompilerOptions) st
 	case tspath.ExtensionDcts, tspath.ExtensionCts, tspath.ExtensionCjs:
 		return tspath.ExtensionCjs
 	default:
+		for _, e := range tspath.ExtraExtensions {
+			if strings.HasSuffix(fileName, e) {
+				return e
+			}
+		}
 		return ""
 	}
 }
diff --git a/pkg/tspath/extension.go b/pkg/tspath/extension.go
index 8e78809fc..25c3c4d69 100644
--- a/pkg/tspath/extension.go
+++ b/pkg/tspath/extension.go
@@ -22,6 +22,8 @@ const (
 )
 
 var (
+	ExtraExtensions                                = []string{}
+	ExtraExtensionless                             = []string{}
 	SupportedDeclarationExtensions                 = []string{ExtensionDts, ExtensionDcts, ExtensionDmts}
 	SupportedTSImplementationExtensions            = []string{ExtensionTs, ExtensionTsx, ExtensionMts, ExtensionCts}
 	supportedTSExtensionsForExtractExtension       = []string{ExtensionDts, ExtensionDcts, ExtensionDmts, ExtensionTs, ExtensionTsx, ExtensionMts, ExtensionCts}
@@ -36,12 +38,33 @@ var (
 	ExtensionsNotSupportingExtensionlessResolution = []string{ExtensionMts, ExtensionDmts, ExtensionMjs, ExtensionCts, ExtensionDcts, ExtensionCjs}
 )
 
+func RegisterSupportedExtension(ext string) {
+	ExtraExtensions = append(ExtraExtensions, ext)
+	AllSupportedExtensions[0] = append(AllSupportedExtensions[0], ext)
+	AllSupportedExtensionsWithJson[0] = append(AllSupportedExtensionsWithJson[0], ext)
+	SupportedTSExtensions[0] = append(SupportedTSExtensions[0], ext)
+	SupportedTSExtensionsWithJson[0] = append(SupportedTSExtensionsWithJson[0], ext)
+	SupportedTSExtensionsFlat = append(SupportedTSExtensionsFlat, ext)
+	SupportedTSExtensionsWithJsonFlat = append(SupportedTSExtensionsWithJsonFlat, ext)
+}
+
+func RegisterSupportedExtensionless(ext string) {
+	ExtraExtensionless = append(ExtraExtensionless, ext)
+}
+
 func ExtensionIsTs(ext string) bool {
 	return ext == ExtensionTs || ext == ExtensionTsx || ext == ExtensionDts || ext == ExtensionMts || ext == ExtensionDmts || ext == ExtensionCts || ext == ExtensionDcts || len(ext) >= 7 && ext[:3] == ".d." && ext[len(ext)-3:] == ".ts"
 }
 
 var extensionsToRemove = []string{ExtensionDts, ExtensionDmts, ExtensionDcts, ExtensionMjs, ExtensionMts, ExtensionCjs, ExtensionCts, ExtensionTs, ExtensionJs, ExtensionTsx, ExtensionJsx, ExtensionJson}
 
+func RegisterExtensionToRemove(ext string) {
+	extensionsToRemove = append(extensionsToRemove, ext)
+}
+func ShouldRemoveExtension(ext string) bool {
+	return slices.Contains(extensionsToRemove, ext)
+}
+
 func RemoveFileExtension(path string) string {
 	// Remove any known extension even if it has more than one dot
 	for _, ext := range extensionsToRemove {
@@ -134,6 +157,9 @@ func GetDeclarationEmitExtensionForPath(path string) string {
 		return ExtensionDts
 	default:
 		ext := GetAnyExtensionFromPath(path, nil, false)
+		if slices.Contains(ExtraExtensions, ext) {
+			return ExtensionDts
+		}
 		if ext != "" {
 			return ".d" + ext + ".ts"
 		}
diff --git a/pkg/vfs/osvfs/os.go b/pkg/vfs/osvfs/os.go
index 1bf3b5b58..a8bbe8a4a 100644
--- a/pkg/vfs/osvfs/os.go
+++ b/pkg/vfs/osvfs/os.go
@@ -58,9 +58,14 @@ var isFileSystemCaseSensitive = func() bool {
 	// As a proxy for case-insensitivity, we check if the current executable exists under a different case.
 	// This is not entirely correct, since different OSs can have differing case sensitivity in different paths,
 	// but this is largely good enough for our purposes (and what sys.ts used to do with __filename).
-	exe, err := os.Executable()
+	// exe, err := os.Executable()
+	// if err != nil {
+	// 	panic(fmt.Sprintf("vfs: failed to get executable path: %v", err))
+	// }
+	// Golar: os.Executable() doesn't work on macOS when compiled to addon
+	exe, err := os.Getwd()
 	if err != nil {
-		panic(fmt.Sprintf("vfs: failed to get executable path: %v", err))
+		panic(fmt.Sprintf("vfs: failed to get cwd path: %v", err))
 	}
 
 	// If the current executable exists under a different case, we must be case-insensitive.
-- 
2.53.0

